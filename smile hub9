-- [[ 1. 古いUIを消去（重複防止） ]]
pcall(function()
    for _, v in ipairs(game:GetService("CoreGui"):GetChildren()) do
        if v.Name == "Orion" or v:FindFirstChild("Main") then
            v:Destroy()
        end
    end
end)

-- [[ 2. サービスと基本変数（一括定義） ]]
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Workspace = workspace
local LP = Players.LocalPlayer

-- [[ グローバル設定テーブル（先に定義） ]]
Config = {}
Config.Aura = {
    KillAura = false,
    VoidAura = false,
    RagdollAura = false,
    BlastAura = false,
    SpinAura = false,
    FlingAura = false,
    SlowLegsAura = false,
    IrritateAura = false,
    Radius = 100,
    Height = 100,
    BlastPower = 5000,
    SpinSpeed = 100,
    FlingPower = 3000,
    SlowLegsSpeed = 5,
    IrritateIntensity = 30
}

EggConfig = {}
EggConfig.Grab = {
    KickGrab = false,
    KillGrab = false,
    VoidGrab = false,
    AnchorGrab = false,
    ThrowEnabled = false,
    ThrowPower = 250
}
EggConfig.Defense = {
    AntiGrab = false,
    AntiVoid = false,
    AntiExplode = false,
    AntiGucci = false,
    AntiRagdoll = false
}
EggConfig.Player = {
    SpeedEnabled = false,
    SpeedValue = 16,
    JumpEnabled = false,
    JumpValue = 50
}

WingsTargetPlayer = nil

-- [[ 3. Orion Library ロード ]]
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Siro-script/FtaP.90hub/refs/heads/main/Orion"))()

-- [[ 4. ウィンドウ作成 ]]
local Window = OrionLib:MakeWindow({Name = "SmileHub", IntroText = "Physical Chaos Loaded", HidePremium = false, SaveConfig = false})

-- レインボーライン機能ロジック --

local originalColors

if ReplicatedStorage:FindFirstChild("DataEvents") and ReplicatedStorage.DataEvents:FindFirstChild("UpdateLineColorsEvent") then

originalColors = {}

for i = 1, 10 do

table.insert(originalColors, Color3.new(1,1,1))

end

end

local ToggleState = false

local hueOffset = 0

local function FireRainbowColors()

if ReplicatedStorage:FindFirstChild("DataEvents") and ReplicatedStorage.DataEvents:FindFirstChild("UpdateLineColorsEvent") then

local cs = ColorSequence.new{

ColorSequenceKeypoint.new(0, Color3.fromHSV((0+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.1, Color3.fromHSV((0.1+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.2, Color3.fromHSV((0.2+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.3, Color3.fromHSV((0.3+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.4, Color3.fromHSV((0.4+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.5, Color3.fromHSV((0.5+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.6, Color3.fromHSV((0.6+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.7, Color3.fromHSV((0.7+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.8, Color3.fromHSV((0.8+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(0.9, Color3.fromHSV((0.9+hueOffset)%1,1,1)),

ColorSequenceKeypoint.new(1, Color3.fromHSV((1+hueOffset)%1,1,1))

}

ReplicatedStorage.DataEvents.UpdateLineColorsEvent:FireServer(cs, cs.Keypoints[1].Value, cs.Keypoints[2].Value, cs.Keypoints[3].Value, cs.Keypoints[4].Value, cs.Keypoints[5].Value, cs.Keypoints[6].Value, cs.Keypoints[7].Value, cs.Keypoints[8].Value, cs.Keypoints[9].Value)

end

end

RunService.Heartbeat:Connect(function()

if ToggleState then

hueOffset = (hueOffset + 0.005) % 1

FireRainbowColors()

end

end)

--- 翼MODロジック (WINGS MOD) ---

local Enabled = false

local Spacing = 0.0

local WingHeight = 2.0  

local BackOffset = 0.5  

local SideOffset = 0.0

local WaveAmplitude = 2.5

local WaveSpeed = 5.0

local TwistAngle = 20.0

local TwistSpeed = 5.0

local TwistWave = 0.5

local TargetSet = "Sparkler"

local defaultMaxZoom = 12

local maxZoom5000Enabled = false

local TargetSets = {

["GlassBox"] = {"GlassBoxGray"},

["Pallet"] = {"PalletLightBrown"},

["Sparkler"] = {"FireworkSparkler"},

["Toilet"] = {"ToiletWhite","ToiletGold"},

["Piano"] = {"KeyboardPart", "MusicKeyboard"},

["Stick"] = {"Stick"}

}

local list, loopConn = {}, nil

local tAccum = 0

local function HRP()

if WingsTargetPlayer and WingsTargetPlayer.Character and WingsTargetPlayer.Character:FindFirstChild("HumanoidRootPart") then

return WingsTargetPlayer.Character.HumanoidRootPart

end

local c = LP.Character or LP.CharacterAdded:Wait()

return c:FindFirstChild("HumanoidRootPart")

end

local function getPartFromModel(m)

if m.PrimaryPart then return m.PrimaryPart end

return m:FindFirstChildWhichIsA("BasePart")

end

local function nameInTargets(n)

local t = TargetSets[TargetSet]

if not t then return false end

for _, nm in ipairs(t) do if n == nm then return true end end

return false

end

local function attachPhysics(rec)

local model = rec.model; local part = rec.part

if not model or not part or not part.Parent then return end

for _, p in ipairs(model:GetDescendants()) do

if p:IsA("BasePart") and not p:IsA("BodyMover") then

pcall(function() p:SetNetworkOwner(LP) end)

p.CanCollide, p.CanTouch = false, false

end

end

if not part:FindFirstChild("BodyVelocity") then

local bv = Instance.new("BodyVelocity", part); bv.Name = "BodyVelocity"; bv.MaxForce = Vector3.new(1e8,1e8,1e8); bv.Velocity = Vector3.new(); bv.P = 1e6

end

if not part:FindFirstChild("BodyGyro") then

local bg = Instance.new("BodyGyro", part); bg.Name = "BodyGyro"; bg.MaxTorque = Vector3.new(1e8,1e8,1e8); bg.CFrame = part.CFrame; bg.P = 1e6

end

end

local function detachPhysics(rec)

local model = rec.model; local part = rec.part

if not model or not part then return end

if part:FindFirstChild("BodyVelocity") then part.BodyVelocity:Destroy() end

if part:FindFirstChild("BodyGyro") then part.BodyGyro:Destroy() end

for _, p in ipairs(model:GetDescendants()) do

if p:IsA("BasePart") and not p:IsA("BodyMover") then

p.CanCollide, p.CanTouch = true, true

pcall(function() p:SetNetworkOwner(nil) end)

end

end

end

local function rescan()

for _,r in ipairs(list) do detachPhysics(r) end

list = {}

for _, d in ipairs(Workspace:GetDescendants()) do

if d:IsA("Model") and nameInTargets(d.Name) then

local part = getPartFromModel(d)

if part and not part.Anchored then table.insert(list, {model=d, part=part}) end

elseif d:IsA("BasePart") and nameInTargets(d.Name) then 

if not d.Anchored then table.insert(list, {model=d, part=d}) end

end

end

for i = 1, #list do attachPhysics(list[i]) end

end

local function startLoop()

if loopConn then loopConn:Disconnect() end

tAccum = 0

loopConn = RunService.Heartbeat:Connect(function(dt)

local root = HRP()

if not root or #list == 0 then return end

tAccum = tAccum + dt

local wingsCenter = root.CFrame * CFrame.new(SideOffset, WingHeight, BackOffset)

local WaveConstant = math.pi / #list

for j = 1, #list do

local rec = list[j]; local p = rec.part

if p and p.Parent then

local isRight = (j % 2 == 0); local sign = isRight and 1 or -1

local animationIndex = math.ceil(j / 2)

local distanceIndex = isRight and (j - 2) / 2 or (j - 1) / 2

local xOffset = Spacing * (distanceIndex + 0.2) * sign

local waveOffset = WaveAmplitude * math.sin(WaveSpeed * tAccum + animationIndex * WaveConstant)

local twistOscillation = math.sin(TwistSpeed * tAccum + animationIndex * TwistWave)

local currentTwistAngle = math.rad(TwistAngle * twistOscillation * (distanceIndex / (#list / 2)))

local targetCFrame = wingsCenter * CFrame.Angles(currentTwistAngle, 0, 0) * CFrame.new(xOffset, waveOffset, 0)    

if p:FindFirstChild("BodyVelocity") then p.BodyVelocity.Velocity = (targetCFrame.Position - p.Position) * 20 end    

if p:FindFirstChild("BodyGyro") then p.BodyGyro.CFrame = targetCFrame end    

end    

end

end)

end

--- タブ構成 ---

-- 情報タブ

local InfoTab = Window:MakeTab({

Name = "情報",

Icon = "rbxassetid://4483345998",

PremiumOnly = false

})

InfoTab:AddLabel("クレジット: BGHACKERS / Logfeetst")

InfoTab:AddButton({

Name = "Discordリンクをコピー",

Callback = function()

setclipboard("https://discord.gg/Ghn3cjjHem")

OrionLib:MakeNotification({Name = "コピー完了", Content = "リンクをコピーしたぜ、マスター。", Time = 3})

end

})

-- ライン色タブ

local LineTab = Window:MakeTab({

Name = "ライン色",

Icon = "rbxassetid://4483345998",

PremiumOnly = false

})

LineTab:AddToggle({

Name = "レインボーライン (FTAP専用)",

Default = false,

Callback = function(Value)

ToggleState = Value

if not Value and originalColors then

ReplicatedStorage.DataEvents.UpdateLineColorsEvent:FireServer(originalColors[1],originalColors[2],originalColors[3],originalColors[4],originalColors[5],originalColors[6],originalColors[7],originalColors[8],originalColors[9],originalColors[10])

end

end

})

-- テレポートタブ

local TPTab = Window:MakeTab({

Name = "テレポート",

Icon = "rbxassetid://4483345998",

PremiumOnly = false

})

local function Teleport(coords)

local player = game.Players.LocalPlayer

if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then

player.Character.HumanoidRootPart.CFrame = CFrame.new(coords)

end

end

TPTab:AddSection({Name = "各地点への移動"})

TPTab:AddButton({ Name = "ピンクの家へテレポート", Callback = function() Teleport(Vector3.new(-481.23, -7.35, -149.67)) end })

TPTab:AddButton({ Name = "緑の家へテレポート", Callback = function() Teleport(Vector3.new(-546.02, -7.35, 78.07)) end })

TPTab:AddButton({ Name = "赤い家へテレポート", Callback = function() Teleport(Vector3.new(542.11, 123.34, -91.78)) end })

TPTab:AddButton({ Name = "水色の家へテレポート", Callback = function() Teleport(Vector3.new(496.99, 83.34, -350.96)) end })

-- WINGSタブ

local WingTab = Window:MakeTab({

Name = "WINGS",

Icon = "rbxassetid://6031075927",

PremiumOnly = false

})

WingTab:AddDropdown({

Name = "ターゲット選択",

Default = TargetSet,

Options = {"Stick", "Sparkler","Toilet", "Piano", "GlassBox", "Pallet"},

Callback = function(v) TargetSet = v; if Enabled then rescan() end end

})

WingTab:AddToggle({

Name = "翼 起動/停止",

Default = false,

Callback = function(v)

Enabled = v

if v then rescan(); startLoop() else if loopConn then loopConn:Disconnect() end; for _,rec in ipairs(list) do detachPhysics(rec) end; list = {} end

end

})

WingTab:AddButton({

Name = "Fly GUI v3 起動",

Callback = function()

loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()

end

})

WingTab:AddSlider({ Name = "翼の広さ (Spacing)", Min = 0, Max = 5, Default = 0.2, Increment = 0.1, Callback = function(v) Spacing = v end })

WingTab:AddSlider({ Name = "翼の高さ (Height)", Min = -10, Max = 100, Default = 2, Increment = 1, Callback = function(v) WingHeight = v end })

WingTab:AddToggle({

Name = "カメラズーム 5000",

Default = false,

Callback = function(v)

maxZoom5000Enabled = v

LP.CameraMaxZoomDistance = v and 5000 or defaultMaxZoom

end

})

LP.CameraMode = Enum.CameraMode.Classic

LP.CameraMinZoomDistance = 0.5

-- ESP機能

local ESPEnabled = false

local ESPObjects = {}

local function getHeadshot(userId)

return "https://www.roblox.com/headshot-thumbnail/image?userId="

.. userId .. "&width=150&height=150&format=png"

end

local function createESP(player)

if player == LP then return end

if ESPObjects[player] then return end

local gui = Instance.new("BillboardGui")

gui.Name = "TPHubESP"

gui.Size = UDim2.new(0, 220, 0, 90)

gui.StudsOffset = Vector3.new(0, 2.5, 0)

gui.AlwaysOnTop = true

local frame = Instance.new("Frame")

frame.Size = UDim2.new(1, 0, 1, 0)

frame.BackgroundColor3 = Color3.fromRGB(20,20,20)

frame.BackgroundTransparency = 0.2

frame.BorderSizePixel = 1

frame.Parent = gui

Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)

local img = Instance.new("ImageLabel")

img.Size = UDim2.new(0, 64, 0, 64)

img.Position = UDim2.new(0, 6, 0, 12)

img.BackgroundTransparency = 1

img.Image = getHeadshot(player.UserId)

img.Parent = frame

local nameLabel = Instance.new("TextLabel")

nameLabel.Position = UDim2.new(0, 80, 0, 18)

nameLabel.Size = UDim2.new(1, -90, 0, 22)

nameLabel.BackgroundTransparency = 1

nameLabel.Text = player.DisplayName

nameLabel.TextColor3 = Color3.new(1,1,1)

nameLabel.TextScaled = true

nameLabel.Font = Enum.Font.GothamBold

nameLabel.Parent = frame

local idLabel = Instance.new("TextLabel")

idLabel.Position = UDim2.new(0, 80, 0, 42)

idLabel.Size = UDim2.new(1, -90, 0, 20)

idLabel.BackgroundTransparency = 1

idLabel.Text = "ID : "..player.UserId

idLabel.TextColor3 = Color3.fromRGB(180,180,180)

idLabel.TextScaled = true

idLabel.Font = Enum.Font.Gotham

idLabel.Parent = frame

ESPObjects[player] = gui

local function attach()

if player.Character and player.Character:FindFirstChild("Head") then

gui.Parent = player.Character.Head

end

end

attach()

player.CharacterAdded:Connect(function()

task.wait(1)

attach()

end)

end

local function removeAllESP()

for _, gui in pairs(ESPObjects) do

if gui then gui:Destroy() end

end

ESPObjects = {}

end

Players.PlayerAdded:Connect(function(player)

if ESPEnabled then

createESP(player)

end

end)

-- ESP タブ

local ESPTab = Window:MakeTab({

Name = "ESP",

Icon = "rbxassetid://4483345998",

PremiumOnly = false

})

ESPTab:AddToggle({

Name = "ESP 有効化 (名前 / ID / 顔)",

Default = false,

Callback = function(value)

ESPEnabled = value

if value then

for _, player in ipairs(Players:GetPlayers()) do

createESP(player)

end

else

removeAllESP()

end

end

})

-- 回転機能

local SpinEnabled = false

local SpinSpeed = 10 

RunService.Heartbeat:Connect(function()

if not SpinEnabled then return end

local char = LP.Character

local hrp = char and char:FindFirstChild("HumanoidRootPart")

if hrp then

hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(SpinSpeed), 0)

end

end)

WingTab:AddToggle({

Name = "高速回転 (右スピン)",

Default = false,

Callback = function(v)

SpinEnabled = v

end

})

WingTab:AddSlider({

Name = "回転スピード",

Min = 1,

Max = 20,

Default = 10,

Increment = 1,

Callback = function(v)

SpinSpeed = v

end

})

-- WINGS 対象プレイヤー

local function getPlayerNameList()

local t = {"自分"}

for _, plr in ipairs(Players:GetPlayers()) do

if plr ~= LP then

table.insert(t, plr.Name)

end

end

return t

end

WingTab:AddDropdown({

Name = "WINGS 対象プレイヤー",

Default = "自分",

Options = getPlayerNameList(),

Callback = function(v)

if v == "自分" then

WingsTargetPlayer = nil

else

WingsTargetPlayer = Players:FindFirstChild(v)

end

end

})

-- 竜巻モード

local TornadoEnabled = false

local TORNADO_RADIUS = 13.5

local TORNADO_HEIGHT = 23

local TORNADO_SPIN_SPEED = 4

local tornadoConn = nil

local function startTornado()

if tornadoConn then tornadoConn:Disconnect() end

rescan()  

tornadoConn = RunService.Heartbeat:Connect(function(dt)  

    local root = HRP()  

    if not root or #list == 0 then return end  

    tAccum += dt  

    local baseCF = root.CFrame  

    for i, rec in ipairs(list) do  

        local p = rec.part  

        if p and p.Parent then  

            local heightOffset = (i / #list) * TORNADO_HEIGHT  

            local angle = (tAccum * TORNADO_SPIN_SPEED) + (i * math.pi * 2 / #list)  

            local x = math.cos(angle) * TORNADO_RADIUS  

            local z = math.sin(angle) * TORNADO_RADIUS  

            local targetCFrame = baseCF * CFrame.new(x, heightOffset, z) * CFrame.Angles(0, angle, 0)  

            if p:FindFirstChild("BodyVelocity") then p.BodyVelocity.Velocity = (targetCFrame.Position - p.Position) * 30 end  

            if p:FindFirstChild("BodyGyro") then p.BodyGyro.CFrame = targetCFrame end  

        end  

    end  

end)

end

local function stopTornado()

if tornadoConn then tornadoConn:Disconnect(); tornadoConn = nil end

for _, rec in ipairs(list) do detachPhysics(rec) end  

list = {}

end

WingTab:AddToggle({

Name = "竜巻モード",

Default = false,

Callback = function(v)

TornadoEnabled = v

if v then  

        if loopConn then loopConn:Disconnect() end  

        startTornado()  

    else  

        stopTornado()  

        if Enabled then rescan(); startLoop() end  

    end  

end

})

-- 円形モード

local CircleEnabled = false

local CIRCLE_RADIUS = 13.5

local CIRCLE_HEIGHT = 2

local CIRCLE_SPEED = 10

local circleConn = nil

local function startCircle()

if circleConn then circleConn:Disconnect() end

rescan()  

circleConn = RunService.Heartbeat:Connect(function(dt)  

    local root = HRP()  

    if not root or #list == 0 then return end  

    tAccum += dt  

    local baseCF = root.CFrame  

    for i, rec in ipairs(list) do  

        local p = rec.part  

        if p and p.Parent then  

            local angle = (i / #list) * math.pi * 2 + (tAccum * CIRCLE_SPEED)  

            local x = math.cos(angle) * CIRCLE_RADIUS  

            local z = math.sin(angle) * CIRCLE_RADIUS  

            local targetCFrame = baseCF * CFrame.new(x, CIRCLE_HEIGHT, z) * CFrame.Angles(0, angle, 0)  

            if p:FindFirstChild("BodyVelocity") then p.BodyVelocity.Velocity = (targetCFrame.Position - p.Position) * 30 end  

            if p:FindFirstChild("BodyGyro") then p.BodyGyro.CFrame = targetCFrame end  

        end  

    end  

end)

end

local function stopCircle()

if circleConn then circleConn:Disconnect(); circleConn = nil end

for _, rec in ipairs(list) do detachPhysics(rec) end  

list = {}

end

WingTab:AddToggle({

Name = "円形モード",

Default = false,

Callback = function(v)

CircleEnabled = v

if v then  

        if loopConn then loopConn:Disconnect() end  

        if tornadoConn then tornadoConn:Disconnect() end  

        startCircle()  

    else  

        stopCircle()  

        if Enabled then rescan(); startLoop() end  

    end  

end

})

-- モードカスタム設定

WingTab:AddSection({Name = "竜巻モード設定"})

WingTab:AddSlider({ Name = "竜巻 速度", Min = 1, Max = 50, Default = 4, Increment = 1, Callback = function(v) TORNADO_SPIN_SPEED = v end })

WingTab:AddSlider({ Name = "竜巻 半径", Min = 1, Max = 100, Default = 13, Increment = 1, Callback = function(v) TORNADO_RADIUS = v end })

WingTab:AddSlider({ Name = "竜巻 高さ", Min = 1, Max = 150, Default = 23, Increment = 1, Callback = function(v) TORNADO_HEIGHT = v end })

WingTab:AddSection({Name = "円形モード設定"})

WingTab:AddSlider({ Name = "円形 速度", Min = 1, Max = 50, Default = 10, Increment = 1, Callback = function(v) CIRCLE_SPEED = v end })

WingTab:AddSlider({ Name = "円形 半径", Min = 1, Max = 100, Default = 13, Increment = 1, Callback = function(v) CIRCLE_RADIUS = v end })

WingTab:AddSlider({ Name = "円形 高さ", Min = -20, Max = 100, Default = 2, Increment = 1, Callback = function(v) CIRCLE_HEIGHT = v end })

-- 伸縮コーンモード

local cMaxH, cGrowS, cRadius, cSpeed, cConn, tAcc = 40, 2, 15, 10, nil, 0

WingTab:AddSection({Name = "特殊形状: 伸縮コーン"})

WingTab:AddToggle({

Name = "コーンモード起動",

Default = false,

Callback = function(v)

if v then

if loopConn then loopConn:Disconnect() end

if tornadoConn then tornadoConn:Disconnect() end

if circleConn then circleConn:Disconnect() end

rescan()

cConn = RunService.Heartbeat:Connect(function(dt)

local root = HRP()

if not root or not list or #list == 0 then return end

tAcc += dt

local curH = (math.sin(tAcc * cGrowS) + 1) / 2 * cMaxH

for i, rec in ipairs(list) do

local p = rec.part

if p and p.Parent then

local prog = i / #list

local r = cRadius * (1 - prog)

local h = prog * curH

local ang = (tAcc * (cSpeed / 2)) + (i * math.pi * 2 / #list)

local targetCF = root.CFrame * CFrame.new(math.cos(ang)*r, h, math.sin(ang)*r) * CFrame.Angles(0, ang, 0)

if p:FindFirstChild("BodyVelocity") then p.BodyVelocity.Velocity = (targetCF.Position - p.Position) * 30 end

if p:FindFirstChild("BodyGyro") then p.BodyGyro.CFrame = targetCF end

end

end

end)

else if cConn then cConn:Disconnect() end if Enabled then rescan(); startLoop() end end

end

})

WingTab:AddSlider({Name = "最大高さ", Min = 1, Max = 150, Default = 40, Increment = 1, Callback = function(v) cMaxH = v end})

WingTab:AddSlider({Name = "伸縮スピード", Min = 0, Max = 10, Default = 2, Increment = 0.1, Callback = function(v) cGrowS = v end})

WingTab:AddSlider({Name = "円の広さ", Min = 1, Max = 100, Default = 15, Increment = 1, Callback = function(v) cRadius = v end})

-- カメラジャック

local CamTab = Window:MakeTab({ Name = "カメラ (他人の視点)", Icon = "rbxassetid://4370345144", PremiumOnly = false })

local TargetPlayerName = "自分"

local ViewPlayerDropdown = CamTab:AddDropdown({

Name = "見るプレイヤーを選択",

Options = getPlayerNameList(),

Default = "自分",

Callback = function(v) TargetPlayerName = v end

})

CamTab:AddButton({ Name = "リスト更新", Callback = function() ViewPlayerDropdown:Refresh(getPlayerNameList(), true) end })

CamTab:AddToggle({

Name = "視点ジャック開始",

Default = false,

Callback = function(v)

if v then

local target = Players:FindFirstChild(TargetPlayerName)

if target and target.Character and target.Character:FindFirstChild("Humanoid") then

workspace.CurrentCamera.CameraSubject = target.Character.Humanoid

end

else

workspace.CurrentCamera.CameraSubject = LP.Character:FindFirstChild("Humanoid")

end

end

})

-- [[ 端末判別・制御変数 ]]
local isMobile = (game:GetService("UserInputService").TouchEnabled and not game:GetService("UserInputService").KeyboardEnabled)
local autoFlingEnabled = false
local flingDistance = 100

-- [[ サイレントオート・タブ（統合版） ]]
local SilentAutoTab = Window:MakeTab({
    Name = "サイレントオート",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

SilentAutoTab:AddToggle({
    Name = "セーフ・オートフリング有効化",
    Default = false,
    Callback = function(v)
        autoFlingEnabled = v
        local modeText = isMobile and "モバイル最適化モード" or "PC高精度モード"
        if v then
            OrionLib:MakeNotification({
                Name = "SmileHub", 
                Content = modeText .. "で自分を守りつつフリングを開始したぜ", 
                Time = 2
            })
        end
    end
})

SilentAutoTab:AddSlider({
    Name = "オート索敵範囲",
    Min = 10, Max = 500, Default = 100, Increment = 10,
    ValueName = "Studs",
    Callback = function(v) flingDistance = v end
})

-- [[ 統合フリング実行エンジン ]]
task.spawn(function()
    while task.wait() do
        if autoFlingEnabled then
            pcall(function()
                local lp = game.Players.LocalPlayer
                local char = lp.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                
                if hrp then
                    local target, closest = nil, flingDistance
                    -- ターゲット検索（両OS共通の最適化ロジック） 
                    for _, p in pairs(game.Players:GetPlayers()) do
                        if p ~= lp and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                            local d = (hrp.Position - p.Character.HumanoidRootPart.Position).Magnitude
                            if d < closest then
                                closest = d
                                target = p
                            end
                        end
                    end
                    
                    if target and target.Character:FindFirstChild("HumanoidRootPart") then
                        local tHRP = target.Character.HumanoidRootPart
                        local oldCF = hrp.CFrame
                        
                        -- 端末ごとにフリングの挙動を切り替え
                        if isMobile then
                            -- モバイル用：hookmetamethod環境でも安定する高出力フリング [cite: 11, 12]
                            hrp.CFrame = tHRP.CFrame * CFrame.new(0, 0, 1) * CFrame.Angles(math.rad(90), 0, 0)
                            hrp.Velocity = Vector3.new(1000, 1000, 1000)
                            task.wait(0.1)
                        else
                            -- PC用：CameraClone環境で視点がブレない超高速回転フリング [cite: 1, 7]
                            hrp.CFrame = tHRP.CFrame * CFrame.new(0, 0, 0.5) * CFrame.Angles(0, math.rad(tick()*10000 % 360), 0)
                            hrp.Velocity = Vector3.new(500, 500, 500)
                            task.wait(0.05)
                        end
                        
                        hrp.CFrame = oldCF
                        hrp.Velocity = Vector3.new(0, 0, 0)
                    end
                end
            end)
        end
    end
end)
-- [[ ブロブマン・究極物理ハックタブ ]]
local BlobmanTab = Window:MakeTab({
    Name = "ブロブマン",
    Icon = "rbxassetid://10427807755",
    PremiumOnly = false
})

local selectedPlayerName = nil

local function getBehindPosition(targetHRP, distance)
    return targetHRP.Position - targetHRP.CFrame.LookVector * (distance or 4)
end

local function teleportBlobman(blobman, position)
    pcall(function()
        local part = blobman:FindFirstChild("HumanoidRootPart") or blobman:FindFirstChild("VehicleSeat") or blobman.PrimaryPart
        if part then
            blobman:SetPrimaryPartCFrame(CFrame.new(position + Vector3.new(0, 3, 0)))
        end
    end)
end

local function blobGrabPlayer(player, blobman, side)
    local targetHRP = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not targetHRP then return end
    local detector = (side == "Left") and blobman:FindFirstChild("LeftDetector") or blobman:FindFirstChild("RightDetector")
    local weld = detector and detector:FindFirstChild(side .. "Weld")
    local remote = blobman:FindFirstChild("BlobmanSeatAndOwnerScript") and blobman.BlobmanSeatAndOwnerScript:FindFirstChild("CreatureGrab")
    if detector and weld and remote then
        pcall(function() remote:FireServer(detector, targetHRP, weld) end)
    end
end

local function blobRelease(blobman, side)
    local detector = (side == "Left") and blobman:FindFirstChild("LeftDetector") or blobman:FindFirstChild("RightDetector")
    local remote = blobman:FindFirstChild("BlobmanSeatAndOwnerScript") and blobman.BlobmanSeatAndOwnerScript:FindFirstChild("CreatureGrab")
    if detector and remote then
        pcall(function() remote:FireServer(detector, nil, nil) end)
    end
end

local function findMyBlobman()
    for _, v in ipairs(workspace:GetDescendants()) do
        if v.Name == "CreatureBlobman" and v:FindFirstChild("VehicleSeat") then
            local weld = v.VehicleSeat:FindFirstChild("SeatWeld")
            if weld and weld.Part1 and weld.Part1:IsDescendantOf(LP.Character) then
                return v
            end
        end
    end
    return nil
end

-- --- プレイヤー選択 ---
BlobmanTab:AddSection({ Name = "ターゲット選択" })

local playerNames = {}
local nameMap = {}
local playerDropdown

local function RefreshList()
    playerNames = {}
    nameMap = {}
    for _, p in ipairs(game.Players:GetPlayers()) do
        if p ~= LP then
            local label = p.DisplayName .. " (" .. p.Name .. ")"
            table.insert(playerNames, label)
            nameMap[label] = p
        end
    end
    if playerDropdown then playerDropdown:Refresh(playerNames, true) end
end

playerDropdown = BlobmanTab:AddDropdown({
    Name = "ターゲットを選択",
    Options = playerNames,
    Callback = function(Option) selectedPlayerName = Option end
})

BlobmanTab:AddButton({ Name = "プレイヤーリストを更新", Callback = RefreshList })
RefreshList()
BlobmanTab:AddButton({
    Name = "キック (浮遊＋高速両手Bring)",
    Callback = function()
        local target = nameMap[selectedPlayerName]
        local blobman = findMyBlobman()
        
        if not (target and target.Character and blobman) then 
            OrionLib:MakeNotification({Name = "エラー", Content = "ターゲットかブロブマンが見つからないぜ！", Time = 3})
            return 
        end
        
        local hrp = target.Character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        blobman:PivotTo(hrp.CFrame * CFrame.new(0, 0, 4))
        
        local rootPart = blobman:FindFirstChild("HumanoidRootPart") or blobman:FindFirstChild("VehicleSeat")
        if rootPart then
            local bp = Instance.new("BodyPosition", rootPart)
            bp.MaxForce = Vector3.new(1e6, 1e6, 1e6)
            bp.P = 10000
            bp.Position = rootPart.Position + Vector3.new(0, 80, 0)
            game.Debris:AddItem(bp, 3)
        end

        task.spawn(function()
            OrionLib:MakeNotification({Name = "SmileHub", Content = target.Name .. " をキック中...", Time = 2})
            for i = 1, 30 do
                if not target.Character then break end
                
                blobGrabPlayer(target, blobman, "Left")
                blobGrabPlayer(target, blobman, "Right")
                
                task.wait(0.1)
                
                blobRelease(blobman, "Left")
                blobRelease(blobman, "Right")
                
                task.wait(0.05)
            end
        end)
    end
})

-- --- 全体操作 ---
BlobmanTab:AddSection({ Name = "全体操作 (All)" })

local kickAllActive = false
BlobmanTab:AddToggle({
    Name = "キックオール起動",
    Default = false,
    Callback = function(v)
        kickAllActive = v
        if v then
            task.spawn(function()
                while kickAllActive do
                    local blobman = findMyBlobman()
                    if blobman then
                        for _, plr in ipairs(game.Players:GetPlayers()) do
                            if not kickAllActive then break end
                            if plr == LP then continue end
                            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                teleportBlobman(blobman, getBehindPosition(hrp, 5))
                                task.wait(0.15)
                                blobGrabPlayer(plr, blobman, "Left")
                                blobGrabPlayer(plr, blobman, "Right")
                            end
                        end
                    end
                    task.wait(0.5)
                end
            end)
        end
    end
})

local fastV2Active = false
BlobmanTab:AddToggle({
    Name = "高速オールV2",
    Default = false,
    Callback = function(v)
        fastV2Active = v
        if v then
            task.spawn(function()
                while fastV2Active do
                    local blobman = findMyBlobman()
                    if blobman then
                        for _, plr in ipairs(game.Players:GetPlayers()) do
                            if not fastV2Active then break end
                            if plr == LP then continue end
                            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                teleportBlobman(blobman, getBehindPosition(hrp, 3.5))
                                task.wait(0.08)
                                for i = 1, 6 do
                                    blobGrabPlayer(plr, blobman, "Left")
                                    blobGrabPlayer(plr, blobman, "Right")
                                    task.wait(0.03)
                                    blobRelease(blobman, "Left")
                                    blobRelease(blobman, "Right")
                                    task.wait(0.03)
                                end
                            end
                        end
                    end
                    task.wait(0.25)
                end
            end)
        end
    end
})

-- アンチ機能

local AntiTab = Window:MakeTab({ Name = "アンチ", Icon = "rbxassetid://4483345998", PremiumOnly = false })

local kickGrabEnabled = false

local killGrabEnabled = false

AntiTab:AddToggle({

Name = "アンチ・キック",

Default = false,

Callback = function(v)

kickGrabEnabled = v

if v then

local mt = getrawmetatable(game); local oldIndex = mt.__index; setreadonly(mt, false)

mt.__index = newcclosure(function(t, k)

if kickGrabEnabled and tostring(k) == "Kick" then return function() end end

return oldIndex(t, k)

end)

setreadonly(mt, true)

end

end

})

AntiTab:AddToggle({

Name = "アンチ・キル",

Default = false,

Callback = function(v) killGrabEnabled = v end

})

RunService.Heartbeat:Connect(function()

if killGrabEnabled and LP.Character and LP.Character:FindFirstChildOfClass("Humanoid") then

local hum = LP.Character:FindFirstChildOfClass("Humanoid")

if hum.Health > 0 and hum.Health < 100 then hum.Health = 100 end

hum:SetStateEnabled(Enum.HumanoidStateType.Dead, false)

end

end)

-- [[ プレイヤータブ ]]
local PlayerTab = Window:MakeTab({
    Name = "プレイヤー",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

PlayerTab:AddSection({Name = "ステータス設定"})

PlayerTab:AddSlider({
    Name = "歩行速度 (WalkSpeed)",
    Min = 16,
    Max = 500,
    Default = 16,
    Increment = 1,
    ValueName = "Speed",
    Callback = function(Value)
        EggConfig.Player.SpeedValue = Value
    end
})

PlayerTab:AddToggle({
    Name = "スピードハック有効化",
    Default = false,
    Callback = function(Value)
        EggConfig.Player.SpeedEnabled = Value
    end
})

PlayerTab:AddSlider({
    Name = "ジャンプ力 (JumpPower)",
    Min = 50,
    Max = 500,
    Default = 50,
    Increment = 1,
    ValueName = "Power",
    Callback = function(Value)
        EggConfig.Player.JumpValue = Value
    end
})

PlayerTab:AddToggle({
    Name = "ジャンプハック有効化",
    Default = false,
    Callback = function(Value)
        EggConfig.Player.JumpEnabled = Value
    end
})

task.spawn(function()
    while task.wait() do
        pcall(function()
            local char = LP.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then
                if EggConfig.Player.SpeedEnabled then
                    hum.WalkSpeed = EggConfig.Player.SpeedValue
                end
                if EggConfig.Player.JumpEnabled then
                    hum.UseJumpPower = true
                    hum.JumpPower = EggConfig.Player.JumpValue
                end
            end
        end)
    end
end)

-- ===== オーラ機能タブ =====
local AuraTab = Window:MakeTab({
    Name = "オーラ機能",
    Icon = "rbxassetid://7733793319",
    PremiumOnly = false
})

AuraTab:AddSection({
    Name = "基本オーラ"
})

AuraTab:AddToggle({
    Name = "キルオーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.KillAura = v
    end
})

AuraTab:AddToggle({
    Name = "奈落オーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.VoidAura = v
    end
})

AuraTab:AddToggle({
    Name = "ラグドールオーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.RagdollAura = v
    end
})

AuraTab:AddToggle({
    Name = "ぶっ飛ばしオーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.BlastAura = v
    end
})

AuraTab:AddToggle({
    Name = "回転オーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.SpinAura = v
    end
})

AuraTab:AddToggle({
    Name = "投げ飛ばしオーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.FlingAura = v
    end
})

AuraTab:AddToggle({
    Name = "足遅オーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.SlowLegsAura = v
    end
})

AuraTab:AddToggle({
    Name = "イライラオーラ",
    Default = false,
    Callback = function(v)
        Config.Aura.IrritateAura = v
    end
})

AuraTab:AddSection({
    Name = "パラメータ設定"
})

AuraTab:AddSlider({
    Name = "オーラ半径",
    Min = 10,
    Max = 500,
    Default = 100,
    Increment = 10,
    Callback = function(v)
        Config.Aura.Radius = v
    end
})

AuraTab:AddSlider({
    Name = "オーラ高さ",
    Min = 10,
    Max = 500,
    Default = 100,
    Increment = 10,
    Callback = function(v)
        Config.Aura.Height = v
    end
})

AuraTab:AddSlider({
    Name = "吹っ飛ばし威力",
    Min = 1000,
    Max = 10000,
    Default = 5000,
    Increment = 100,
    Callback = function(v)
        Config.Aura.BlastPower = v
    end
})

AuraTab:AddSlider({
    Name = "回転速度",
    Min = 10,
    Max = 500,
    Default = 100,
    Increment = 10,
    Callback = function(v)
        Config.Aura.SpinSpeed = v
    end
})

AuraTab:AddSlider({
    Name = "投げ飛ばし威力",
    Min = 1000,
    Max = 10000,
    Default = 3000,
    Increment = 100,
    Callback = function(v)
        Config.Aura.FlingPower = v
    end
})

AuraTab:AddSlider({
    Name = "足の遅さ",
    Min = 1,
    Max = 16,
    Default = 5,
    Increment = 1,
    Callback = function(v)
        Config.Aura.SlowLegsSpeed = v
    end
})

AuraTab:AddSlider({
    Name = "イライラ強度",
    Min = 10,
    Max = 100,
    Default = 30,
    Increment = 5,
    Callback = function(v)
        Config.Aura.IrritateIntensity = v
    end
})

-- ===== 掴み機能タブ =====
local GrabTab = Window:MakeTab({
    Name = "掴み機能",
    Icon = "rbxassetid://7733798691",
    PremiumOnly = false
})

GrabTab:AddSection({
    Name = "掴みオプション"
})

GrabTab:AddToggle({
    Name = "キック掴み",
    Default = false,
    Callback = function(Value)
        EggConfig.Grab.KickGrab = Value
    end
})

GrabTab:AddToggle({
    Name = "キル掴み",
    Default = false,
    Callback = function(Value)
        EggConfig.Grab.KillGrab = Value
    end
})

GrabTab:AddToggle({
    Name = "奈落掴み",
    Default = false,
    Callback = function(Value)
        EggConfig.Grab.VoidGrab = Value
    end
})

GrabTab:AddToggle({
    Name = "固定掴み",
    Default = false,
    Callback = function(Value)
        EggConfig.Grab.AnchorGrab = Value
    end
})

GrabTab:AddSection({
    Name = "投げる強さ設定"
})

GrabTab:AddToggle({
    Name = "投げる強さON/OFF",
    Default = false,
    Callback = function(Value)
        EggConfig.Grab.ThrowEnabled = Value
    end
})

GrabTab:AddSlider({
    Name = "投げる強さ",
    Min = 0,
    Max = 1000,
    Default = 250,
    Increment = 10,
    ValueName = "強さ",
    Callback = function(Value)
        EggConfig.Grab.ThrowPower = Value
    end
})

-- ===== 防御機能タブ =====
local DefenseTab = Window:MakeTab({
    Name = "防御機能",
    Icon = "rbxassetid://6034509993",
    PremiumOnly = false
})

DefenseTab:AddSection({ Name = "防御オプション" })

DefenseTab:AddToggle({
    Name = "アンチ掴み",
    Default = false,
    Callback = function(v) EggConfig.Defense.AntiGrab = v end
})

DefenseTab:AddToggle({
    Name = "アンチ奈落",
    Default = false,
    Callback = function(v) EggConfig.Defense.AntiVoid = v end
})

DefenseTab:AddToggle({
    Name = "アンチラグドール",
    Default = false,
    Callback = function(v) EggConfig.Defense.AntiRagdoll = v end
})

task.spawn(function()
    while task.wait(0.1) do
        pcall(function()
            local character = game.Players.LocalPlayer.Character
            if not character then return end

            if EggConfig.Defense.AntiRagdoll then
                local hum = character:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                    hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
                end
            end

            if EggConfig.Defense.AntiGrab then
                for _, obj in ipairs(character:GetDescendants()) do
                    if obj:IsA("Weld") or obj:IsA("ManualWeld") or obj:IsA("Snap") then
                        if not obj.Part0:IsDescendantOf(character) or not obj.Part1:IsDescendantOf(character) then
                            obj:Destroy()
                        end
                    end
                end
            end

            if EggConfig.Defense.AntiVoid then
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if hrp and hrp.Position.Y < -50 then
                    hrp.Velocity = Vector3.new(0, 0, 0)
                    hrp.CFrame = CFrame.new(0, 50, 0)
                    OrionLib:MakeNotification({Name = "防御", Content = "奈落から救出したぜ！", Time = 1})
                end
            end
        end)
    end
end)

-- [[ 自動バリア破壊タブ ]]
local BarrierTab = Window:MakeTab({
    Name = "自動バリア破壊",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

local function executeBarrierBreak(useTP)
    return pcall(function()
        local barrierNames = {"Barrier", "Glass", "Wall", "Border"}
        for _, name in ipairs(barrierNames) do
            for _, obj in ipairs(workspace:GetDescendants()) do
                if obj:IsA("BasePart") and obj.Name:find(name) then
                    if useTP and LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
                        LP.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -2)
                    end
                    obj.CanCollide = false
                    obj.Transparency = 0.5
                end
            end
        end
        OrionLib:MakeNotification({Name = "システム", Content = "バリア破壊を試行したぜ！", Time = 1})
    end)
end

BarrierTab:AddSection({ Name = "自動実行オプション" })

_G.AutoBarrierTP = false
BarrierTab:AddToggle({
    Name = "バリア破壊 (TPあり)",
    Default = false,
    Callback = function(Value)
        _G.AutoBarrierTP = Value
        if Value then
            task.spawn(function()
                while _G.AutoBarrierTP do
                    executeBarrierBreak(true)
                    task.wait(1.5)
                end
            end)
        end
    end
})

_G.AutoBarrierNoTP = false
BarrierTab:AddToggle({
    Name = "バリア破壊 (NoTP/Beta)",
    Default = false,
    Callback = function(Value)
        _G.AutoBarrierNoTP = Value
        if Value then
            task.spawn(function()
                while _G.AutoBarrierNoTP do
                    executeBarrierBreak(false)
                    task.wait(1.5)
                end
            end)
        end
    end
})

BarrierTab:AddSection({ Name = "手動コントロール" })

BarrierTab:AddButton({
    Name = "今すぐ単発実行",
    Callback = function()
        executeBarrierBreak(false)
    end
})

-- [[ 制御変数 ]]
local SaturnEnabled = false
local LP = game.Players.LocalPlayer
local myName = LP.Name

local FunTab = Window:MakeTab({
    Name = "楽しい",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- 1. 開発メンバーセクション
FunTab:AddSection({ Name = "SmileHub 開発メンバー" })
FunTab:AddLabel("id : gegdgmvs35..表示名suu")
FunTab:AddLabel("id : Hello838338383")
FunTab:AddLabel("id : MidoriMiroru_73816")
FunTab:AddLabel("id : ftapplayer_LOL")

-- 2. 土星エフェクトセクション（改造版：全モード同時起動 ＆ アイテム強奪）
FunTab:AddSection({ Name = "神の力：土星エフェクト（アイテム強奪）" })
FunTab:AddToggle({
    Name = "土星エフェクト (ALLモード同時起動)",
    Default = false,
    Callback = function(v) 
        SaturnEnabled = v 
        if v then
            OrionLib:MakeNotification({Name = "SmileHub", Content = "全アイテムの所有権を奪取し、土星軌道を生成するぜ！", Time = 3})
        end
    end
})

-- 3. お珍珍MODオプション（保持）
FunTab:AddSection({ Name = "お珍珍MODオプション" })
FunTab:AddToggle({ Name = "ぼっきしたちんちん", Default = false, Callback = function(v) end })
FunTab:AddToggle({ Name = "普通のお珍珍", Default = false, Callback = function(v) end })

-- 4. 特殊アクション（保持）
FunTab:AddSection({ Name = "特殊アクション" })
FunTab:AddButton({ Name = "UFO落とし", Callback = function() breakObject("UFO") end })
FunTab:AddButton({ Name = "列車落とし", Callback = function() breakObject("Train") end })

-- 5. 荒らし機能（保持）
FunTab:AddSection({ Name = "荒らし機能" })
local bringingEnabled = false
FunTab:AddToggle({ Name = "ゲロかけALL (Bring All)", Default = false, Callback = function(v) bringingEnabled = v end })

-- [[ 統合土星ロジック：所有権奪取 + 2モード同時実行 ]]
task.spawn(function()
    local parts = {} -- 予備のパーツ
    local c = { Speed = 2, Size = 12, Height = 5, Back = 0, WaveSpeed = 2, WaveAmp = 2 }
    
    -- 回転用シェイプ
    local shape = {}
    for i = 1, 36 do
        local angle = math.rad(i * 10)
        table.insert(shape, Vector3.new(math.cos(angle), 0, math.sin(angle)))
    end

    while task.wait() do
        if SaturnEnabled then
            local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local time = tick()
                local fws = {}
                local allMyItems = {}

                -- --- アイテム強奪ロジック：ここにお前のコードを完全統合 ---
                local plotsFolder = workspace:FindFirstChild("Plots")
                local plotItemsFolder = workspace:FindFirstChild("PlotItems")
                local spawnedToys = workspace:FindFirstChild(myName .. "SpawnedInToys")

                if spawnedToys then
                    for _, item in ipairs(spawnedToys:GetChildren()) do table.insert(allMyItems, item) end
                end

                if plotsFolder and plotItemsFolder then
                    for _, plot in ipairs(plotsFolder:GetChildren()) do
                        local sign = plot:FindFirstChild("PlotSign")
                        local ownerObj = sign and (sign:FindFirstChild("ThisPlotsOwners") or sign:FindFirstChild("Owner"))
                        if ownerObj then
                            local val = ownerObj:FindFirstChild("Value") or ownerObj
                            local data = val:FindFirstChild("Data") or val
                            if (data:IsA("StringValue") and data.Value == myName) then
                                local targetFolder = plotItemsFolder:FindFirstChild(plot.Name)
                                if targetFolder then
                                    for _, item in ipairs(targetFolder:GetChildren()) do table.insert(allMyItems, item) end
                                end
                                break
                            end
                        end
                    end
                end

                for _, item in ipairs(workspace:GetChildren()) do
                    local ownerValue = item:FindFirstChild("Owner") or item:FindFirstChild("PartOwner")
                    if item:IsA("Model") and ownerValue and ownerValue:IsA("StringValue") and ownerValue.Value == myName then
                        if not table.find(allMyItems, item) then table.insert(allMyItems, item) end
                    end
                end

                for _, item in ipairs(allMyItems) do
                    if item:IsA("Model") and item.PrimaryPart then
                        table.insert(fws, item)
                        -- 所有権を自分にセット
                        for _, part in ipairs(item:GetDescendants()) do
                            if part:IsA("BasePart") then pcall(function() part:SetNetworkOwner(LP) end) end
                        end
                    end
                end
                -- --- アイテム強奪終了 ---

                -- 強奪したアイテム + 予備パーツを軌道に乗せる
                local totalItems = #fws
                for i, item in ipairs(fws) do
                    local pos = Vector3.zero
                    local ratio = i / totalItems
                    
                    -- 同時起動：半分はSphere、半分はRotateにする、もしくはお前の好みに合わせて合成
                    if i % 2 == 0 then
                        -- Sphereモードの動き
                        local phi = math.acos(-1 + (2 * i) / totalItems)
                        local theta = math.sqrt(totalItems * math.pi) * phi + time * c.Speed
                        pos = Vector3.new(c.Size * math.cos(theta) * math.sin(phi), c.Size * math.sin(theta) * math.sin(phi) + c.Height, c.Size * math.cos(phi))
                    else
                        -- Rotateモードの動き
                        local speed, totalPoints = c.Speed, #shape
                        local cycle = (time * speed * 0.1 + ratio) % 1
                        local currentStep = cycle * totalPoints
                        local idx1 = math.floor(currentStep) % totalPoints + 1
                        local idx2 = (math.floor(currentStep) + 1) % totalPoints + 1
                        local p = shape[idx1]:Lerp(shape[idx2], currentStep % 1)
                        local rotAngle = time * speed * 0.3
                        local rotX = p.X * math.cos(rotAngle) - p.Z * math.sin(rotAngle)
                        local rotZ = p.X * math.sin(rotAngle) + p.Z * math.cos(rotAngle)
                        pos = Vector3.new(rotX * c.Size, c.Height + (math.sin(time * c.WaveSpeed + i * 0.5) * c.WaveAmp), rotZ * c.Size)
                    end
                    
                    if item.PrimaryPart then
                        item:SetPrimaryPartCFrame(hrp.CFrame * CFrame.new(pos))
                    end
                end
            end
        end
    end
end)-- [[ LOOPタブ：Blitz物理・リスキル統合管理 ]]
local LoopTab = Window:MakeTab({
    Name = "LOOP",
    Icon = "rbxassetid://10427807755",
    PremiumOnly = false
})

-- 制御変数（Blitzロジック用）
local selectedLoopPlayers = {}
local isLoopKilling = false
local isLoopBringing = false
local isLoopKicking = false
local Noclipping = false
local NoclipConnection = nil

-- --- Blitz内部関数 (物理・ネットワーク制御) ---

local function lookAt(from, to)
    local direction = (to - from).Unit
    local right = direction:Cross(Vector3.new(0, 1, 0))
    local up = right:Cross(direction)
    return CFrame.fromMatrix(from, right, up)
end

local function EnableNoclip()
    if not NoclipConnection then
        Noclipping = false
        NoclipConnection = game:GetService("RunService").Stepped:Connect(function()
            if Noclipping == false and LP.Character then
                for _, part in pairs(LP.Character:GetChildren()) do
                    if part:IsA("BasePart") and part.CanCollide and part.Name ~= "floatName" then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end
end

local function DisableNoclip()
    if NoclipConnection then
        NoclipConnection:Disconnect()
        NoclipConnection = nil
    end
    Noclipping = true
end

local function SNOWship(part)
    local GrabEvents = game:GetService("ReplicatedStorage"):WaitForChild("GrabEvents", 3)
    if not GrabEvents then return end
    local SetNetworkOwner = GrabEvents:WaitForChild("SetNetworkOwner", 3)
    
    if part and typeof(part) == "Instance" and SetNetworkOwner then
        local distance = LP:DistanceFromCharacter(part.Position)
        if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") and distance <= 30 then
            SetNetworkOwner:FireServer(part, lookAt(LP.Character.HumanoidRootPart.Position, part.Position))
        end
    end
end

local function CreateSkyVelocity(part)
    if not part:FindFirstChild("SkyVelocity") then
        local bv = Instance.new("BodyVelocity", part)
        bv.Name = "SkyVelocity"
        bv.Velocity = Vector3.new(0, 100000000000000, 0)
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    end
end

local function CheckNetworkOwnership(player)
    if player and player.Character then
        local head = player.Character:FindFirstChild("Head")
        if head and head:FindFirstChild("PartOwner") and head.PartOwner.Value == LP.Name then
            return true
        end
    end
    return false
end

-- --- ターゲット管理セクション ---
LoopTab:AddSection({ Name = "ターゲット管理" })

local LoopPlayerDropdown
local SelectedLabel = LoopTab:AddLabel("Selected: None")

local function UpdateSelectedList()
    if #selectedLoopPlayers == 0 then
        SelectedLabel:Set("Selected: None")
    else
        SelectedLabel:Set("Selected: " .. table.concat(selectedLoopPlayers, ", "))
    end
end

local function UpdateLoopPlayerList()
    local options = {}
    for _, player in pairs(game.Players:GetPlayers()) do
        if player ~= LP and not table.find(selectedLoopPlayers, player.Name) then
            table.insert(options, player.Name .. " (" .. player.DisplayName .. ")")
        end
    end
    table.sort(options)
    if LoopPlayerDropdown then LoopPlayerDropdown:Refresh(options, true) end
end

LoopPlayerDropdown = LoopTab:AddDropdown({
    Name = "ターゲットを選択して追加",
    Options = {},
    Callback = function(value)
        local playerName = string.split(value, " ")[1]
        if not table.find(selectedLoopPlayers, playerName) then
            table.insert(selectedLoopPlayers, playerName)
            UpdateSelectedList()
            UpdateLoopPlayerList()
        end
    end
})

LoopTab:AddButton({
    Name = "選択をリセット",
    Callback = function()
        selectedLoopPlayers = {}
        UpdateSelectedList()
        UpdateLoopPlayerList()
    end
})

-- --- LOOPアクション実行セクション ---
LoopTab:AddSection({ Name = "Blitz 物理ハック・ループ実行" })

-- 1. LOOP KILL
LoopTab:AddToggle({
    Name = "ループ・キル (Kill Selected)",
    Default = false,
    Callback = function(v)
        isLoopKilling = v
        if v then
            task.spawn(function()
                while isLoopKilling do
                    for i = #selectedLoopPlayers, 1, -1 do
                        if not isLoopKilling then break end
                        local target = game.Players:FindFirstChild(selectedLoopPlayers[i])
                        if target and target.Character then
                            local root = target.Character:FindFirstChild("HumanoidRootPart")
                            local hum = target.Character:FindFirstChildOfClass("Humanoid")
                            if root and hum and hum.Health > 0 then
                                EnableNoclip()
                                SNOWship(root)
                                if CheckNetworkOwnership(target) then
                                    CreateSkyVelocity(root)
                                    local ev = game:GetService("ReplicatedStorage"):FindFirstChild("GrabEvents")
                                    if ev then ev.DestroyGrabLine:FireServer(root) end
                                end
                                hum:ChangeState(Enum.HumanoidStateType.Dead)
                            end
                        end
                    end
                    task.wait(0.2)
                end
                DisableNoclip()
            end)
        end
    end
})

-- 2. LOOP BRING
LoopTab:AddToggle({
    Name = "ループ・ブリング (Bring Selected)",
    Default = false,
    Callback = function(v)
        isLoopBringing = v
        if v then
            task.spawn(function()
                while isLoopBringing do
                    for i = #selectedLoopPlayers, 1, -1 do
                        if not isLoopBringing then break end
                        local target = game.Players:FindFirstChild(selectedLoopPlayers[i])
                        local myHRP = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                        if target and target.Character and myHRP then
                            local root = target.Character:FindFirstChild("HumanoidRootPart")
                            if root then
                                EnableNoclip()
                                SNOWship(root)
                                if CheckNetworkOwnership(target) then
                                    root.CFrame = myHRP.CFrame * CFrame.new(0, 0, -3)
                                end
                            end
                        end
                    end
                    task.wait(0.2)
                end
                DisableNoclip()
            end)
        end
    end
})

-- 3. LOOP KICK
LoopTab:AddToggle({
    Name = "ループ・キック (Kick Selected)",
    Default = false,
    Callback = function(v)
        isLoopKicking = v
        if v then
            task.spawn(function()
                while isLoopKicking do
                    for i = #selectedLoopPlayers, 1, -1 do
                        if not isLoopKicking then break end
                        local target = game.Players:FindFirstChild(selectedLoopPlayers[i])
                        if target and target.Character then
                            local root = target.Character:FindFirstChild("HumanoidRootPart")
                            if root then
                                EnableNoclip()
                                SNOWship(root)
                                if CheckNetworkOwnership(target) or root.AssemblyLinearVelocity.Magnitude > 500 then
                                    CreateSkyVelocity(root)
                                    local ev = game:GetService("ReplicatedStorage"):FindFirstChild("GrabEvents")
                                    if ev then ev.DestroyGrabLine:FireServer(root) end
                                end
                            end
                        end
                    end
                    task.wait(0.2)
                end
                DisableNoclip()
            end)
        end
    end
})

-- 初期リスト読み込み
UpdateLoopPlayerList()
game.Players.PlayerAdded:Connect(UpdateLoopPlayerList)
game.Players.PlayerRemoving:Connect(UpdateLoopPlayerList)
-- [[ 最後に1回だけ初期化 ]]
OrionLib:Init()

task.delay(0.5, function()
    OrionLib:MakeNotification({ Name = "警告", Content = "現在 Stick 以外は使用できません", Time = 4 })
end)
